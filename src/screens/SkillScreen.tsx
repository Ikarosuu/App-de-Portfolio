import { View, Text } from 'react-native'
import { StatusBar } from 'expo-status-bar'

export default function SkillScreen() {
    return(
        <View>
            <Text>Skill Work!</Text>
            <StatusBar style='auto'/>
        </View>
    )
}